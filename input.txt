
# Input file for pore and grain size distribution analysis
# Supported formats: .raw (binary), .tif / .tiff (stack)
# nx, ny, nz are required only for .raw files; omit them for .tif/.tiff

filename    = geopack.raw
nx          = 750       # required for .raw only
ny          = 750       # required for .raw only
nz          = 750       # required for .raw only
pore_label  = 1
solid_label = 0       # for non-binary images, all labels != solid_label are treated as pore
resolution  = 1.66    # voxel size in micrometer
num_threads = 8
cdf         = false   # cumulative distribution function (fraction of pore volume <= radius r)
measure     = pore    # 'pore' for pore size distribution, 'grain' for grain size distribution
save_fig    = psd_plot.pdf  # .pdf or .png
